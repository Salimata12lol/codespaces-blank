def resturantinfo(resturant_tables):
    #try and find out any information for the first row
    https://docs.python.org/3/library/stdtypes.html#string-methods
    resturantinfo=
    for, table (resturant_tables[0][1:]):
        capacity=int(table.split[1].strip) # Extract capacity (e.g.,2)
        resturantinfo[i+1]={'id': resturantinfo_id, capacity:capacity}
    return resturantinfo
